# Max Ganz II — Redshift White Papers Landing Page

A modern, single-file landing page selling six Amazon Redshift deep-dive white papers
from Max Ganz II ($297 each). Built as a self-contained `index.html` for instant Netlify deploy.

## Deploy (Netlify)
- `index.html` is the static site; `netlify.toml` sets publish dir + SPA redirect.
- Connect this repo in Netlify → it deploys automatically.

> Note: all testimonials are fictional samples. Checkout is a mock button (no payment).
